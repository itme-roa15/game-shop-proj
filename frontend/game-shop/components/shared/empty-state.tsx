import { PackageOpen } from "lucide-react";
export function EmptyState({ title, description, action }: { title: string; description: string; action?: React.ReactNode }) { return <div className="col-span-full grid min-h-64 place-items-center rounded-lg border border-dashed border-border bg-surface/40 p-8 text-center"><div><PackageOpen className="mx-auto mb-4 size-10 text-text-muted" /><h2 className="heading text-xl font-semibold">{title}</h2><p className="mt-2 max-w-md text-sm text-text-secondary">{description}</p>{action && <div className="mt-5">{action}</div>}</div></div>; }
