export function PageHeading({ eyebrow, title, description }: { eyebrow?: string; title: string; description?: string }) { return <header>{eyebrow && <p className="mb-2 text-sm font-medium text-accent">{eyebrow}</p>}<h1 className="heading text-3xl font-bold tracking-[-.02em] sm:text-4xl">{title}</h1>{description && <p className="mt-3 max-w-2xl text-text-secondary">{description}</p>}</header>; }
