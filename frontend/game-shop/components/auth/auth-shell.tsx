import { Gamepad2, ShieldCheck, Zap } from "lucide-react";
export function AuthShell({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <div className="page-shell grid min-h-[calc(100vh-4rem)] items-center gap-10 py-12 lg:grid-cols-2"><section className="hidden lg:block"><span className="inline-flex rounded-full border border-accent/30 bg-accent/10 px-3 py-1 text-sm text-accent">Player access</span><h1 className="heading mt-5 max-w-lg text-5xl font-bold tracking-[-.03em]">Your next great session starts here.</h1><p className="mt-5 max-w-md text-lg text-text-secondary">Secure your gear, track orders, and get back to what matters: playing.</p><div className="mt-8 flex gap-6 text-sm text-text-secondary"><span className="flex items-center gap-2"><ShieldCheck className="size-4 text-success" />Secure checkout</span><span className="flex items-center gap-2"><Zap className="size-4 text-accent" />Fast ordering</span></div></section><section className="mx-auto w-full max-w-md rounded-lg border border-border bg-surface p-6 shadow-[0_4px_24px_rgba(0,0,0,.4)] sm:p-8"><Gamepad2 className="mb-5 size-8 text-accent" /><h2 className="heading text-3xl font-bold">{title}</h2><p className="mt-2 text-text-secondary">{description}</p><div className="mt-7">{children}</div></section></div>; }
